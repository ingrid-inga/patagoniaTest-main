server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway2.1
          uri: https://github.com/ingrid-inga/patagoniaTest-main.git
          search-paths: config-data
  application:
    name: config-service